# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/core.kai.io_versions.yaml
- bases/core.kai.io_configs.yaml
- bases/core.kai.io_routers.yaml
- bases/core.kai.io_apps.yaml
- bases/ai.kai.io_inferenceservices.yaml
- bases/ai.kai.io_modelruntimes.yaml
- bases/ai.kai.io_inferencepipelines.yaml
#+kubebuilder:scaffold:crdkustomizeresource

patchesStrategicMerge:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
#- patches/webhook_in_versions.yaml
#- patches/webhook_in_configs.yaml
#- patches/webhook_in_routers.yaml
#- patches/webhook_in_apps.yaml
#- patches/webhook_in_inferenceservices.yaml
#- patches/webhook_in_modelruntimes.yaml
#- path: patches/webhook_in_ai_inferencepipelines.yaml
#+kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable cert-manager, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- patches/cainjection_in_versions.yaml
#- patches/cainjection_in_configs.yaml
#- patches/cainjection_in_routers.yaml
#- patches/cainjection_in_apps.yaml
#- patches/cainjection_in_inferenceservices.yaml
#- patches/cainjection_in_modelruntimes.yaml
#- path: patches/cainjection_in_ai_inferencepipelines.yaml
#+kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
